name: Lint & Test
on: [push]

jobs:
  lint-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install

      # - name: Lint
      #   run: npm run lint

      - name: Test
        run: npm run test
